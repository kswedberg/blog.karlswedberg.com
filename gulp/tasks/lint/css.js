let gulp = require('gulp');
// var checkCSS = require('gulp-check-unused-css');

gulp.task('lint:css', () => {
  return gulp;
  // return gulp.src(['public/assets/css/*.css', 'public/**/*.html'])
  // .pipe(checkCSS());
});
